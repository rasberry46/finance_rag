# Core retrieval
faiss-cpu>=1.8
rank-bm25>=0.2
sentence-transformers>=3.0
numpy>=1.26

# PDF ingestion
pdfplumber>=0.11
pypdf>=5.0

# Sample PDF generation
reportlab>=4.0

# AWS Bedrock (Titan + Claude) — needed only when RAG_PROVIDER=bedrock
boto3>=1.34

# Testing
pytest>=8.0

# Demo UI
streamlit>=1.30

# --- Continuous S3 ingestion (production, optional) ---
# opensearch-py>=2.4        # managed vector store with native BM25 + kNN

# --- Agentic layer (next step) ---
langgraph>=0.2
# langchain-aws>=0.2

# --- Optional convenience ---
# python-dotenv>=1.0        # auto-load .env
